import 'package:flutter/material.dart';

class FundDetailPage extends StatelessWidget {
  const FundDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Fund detail page'),
    );
  }
}
